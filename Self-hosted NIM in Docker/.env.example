# NGC container-pull key (different from build.nvidia.com hosted API key!)
# Create at: https://ngc.nvidia.com  ->  Setup  ->  Generate Personal API Key
# Scope: pick "NGC Catalog" so it can pull nvcr.io/nim/* images.
NGC_API_KEY=nvapi-REPLACE_ME
